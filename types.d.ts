export type NavLinkType = {
  name: string;
  link: string;
  linkIcon?: IconType;
};

export type ExperienceType = {
  title: string,
  date: string,
  company: string,
  place: string,
  description: string[]
}
